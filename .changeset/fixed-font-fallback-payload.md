---
"@figit/dom-to-figma": patch
---

Expose text-run code points to font loaders and emit resolved font families in
Figma text payloads, keeping declared fonts consistent with fallback glyph and
metric data.
