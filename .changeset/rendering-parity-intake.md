---
"@figit/dom-to-figma": patch
---

Improve CSS border, gradient, shadow, filter, blur, outline, and fractional frame rendering parity while preserving fork-specific traversal, text sizing, font, and image behavior.
